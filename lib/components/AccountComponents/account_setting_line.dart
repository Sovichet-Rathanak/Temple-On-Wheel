import 'package:flutter/material.dart';

class AccountsettingBtnLine extends StatelessWidget {
  const AccountsettingBtnLine({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: BoxDecoration(
        border: Border(
          bottom: BorderSide(
            color: Colors.black
          )
        )
      ),
    );
  }
}